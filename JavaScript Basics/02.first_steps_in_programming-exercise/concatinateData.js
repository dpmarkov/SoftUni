function concatinateData(a,b,c,d){
    let fName = a;
    let lName = b;
    let age = Number(c);
    let town = d;

    console.log(`You are ${fName} ${lName}, a ${age}-years old person from ${town}.`);
}

concatinateData("Dimitar", "Markov", "29", "Varna");